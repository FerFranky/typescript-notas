//Enums
enum Roles {
    User = 'User',
    Admin = 'Admin',
    SuperAdmin = 'SuperAdmin'
}

// console.log(Roles.Admin);

// Objects

const roles = {
    User: 0,
    Admin: 1,
    SuperAdmin: 2
}

console.log(roles.SuperAdmin);
